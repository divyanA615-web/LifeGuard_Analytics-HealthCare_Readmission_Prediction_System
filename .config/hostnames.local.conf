# Local Domain URL configurator.
# Used by docker-compose to inject canonical URLs into backend runtime.
# Avoids accidental localhost misroutes across teams branching.

SERVICE_FRONTEND_URL=http://localhost:8081
SERVICE_BACKEND_URL=http://localhost:8080

# Secure tunnel endpoint accepted to represent the same backend
SERVICE_BACKEND_DOMAIN=local.tunnel.lifeguard.dev
