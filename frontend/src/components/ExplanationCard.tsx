import React from "react";
import { Paper, Typography, Stack, Chip } from "@mui/material";

interface ExplanationCardProps {
  text: string;
}

export function ExplanationCard({ text }: ExplanationCardProps): React.ReactElement {
  return (
    <Paper variant="outlined" sx={{ p: 2 }}>
      <Typography variant="body1" sx={{ whiteSpace: "pre-wrap" }}>
        {text}
      </Typography>
      <Stack direction="row" spacing={1} sx={{ mt: 2 }}>
        <Chip label="Generated by Nemotron-3-Ultra" size="small" color="primary" />
        <Chip label="PHI-safe" size="small" color="success" variant="outlined" />
      </Stack>
    </Paper>
  );
}
